## Code style (enforced by convention, not all by lint)

- Tabs for indentation, single quotes, no unnecessary trailing semicolons.
- Prefer arrow functions over `function` declarations.
- Use modern JavaScript/TypeScript (latest ECMAScript features).
- Write concise, readable, maintainable code.